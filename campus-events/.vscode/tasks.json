{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Flask App",
      "type": "shell",
      "command": "${workspaceFolder}/venv/bin/python ${workspaceFolder}/app.py",
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}
